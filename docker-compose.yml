services:
  app:
    build:
      context: './docker'
      dockerfile: Dockerfile
    volumes:
      - ./src:/var/www/html
  nginx:
    image: nginx:latest
    ports:
      - 8080:80
    volumes:
      - ./src:/var/www/html
      - ./docker/nginx:/etc/nginx/conf.d
    depends_on:
      - app
