version: 0.1

phases:
  build:
    commands:
      - aws deploy push --region ap-northeast-1 --appllication-name sample-cicd --s3-location s3://sample-codebuild-artifact-20260419/sample.zip --source src
artifacts:
  files:
    - '**/*'
    base-directory: src
    name: sample.zip
